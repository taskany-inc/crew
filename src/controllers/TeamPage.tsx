import { TeamProfile } from '../components/groups/TeamProfile';
import { LayoutMain } from '../components/layout/LayoutMain';

export const TeamPage = () => {
    return (
        <LayoutMain>
            <TeamProfile />
        </LayoutMain>
    );
};
